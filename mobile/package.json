{
  "name": "eisy-myanmar-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Eisy Myanmar — Capacitor Android shell for the web dashboard",
  "scripts": {
    "build": "node scripts/prepare-web.js",
    "cap:sync": "npm run build && npx cap sync",
    "cap:open": "npx cap open android",
    "cap:run": "npm run cap:sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
