/**
 * Hooks barrel.
 */
(function (root) {
  'use strict';

  root.EisyHooks = root.EisyHooks || {};
  root.EisyHooks.ready = true;
  root.EisyHooks.version = 'step4';
})(typeof globalThis !== 'undefined' ? globalThis : window);
