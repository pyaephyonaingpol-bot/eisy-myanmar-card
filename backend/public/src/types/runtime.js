/**
 * Runtime type namespace for classic scripts (optional helpers / enums mirrors).
 * Prefer the `.d.ts` files for editor autocomplete.
 */
(function (root) {
  'use strict';
  root.Eisy = root.Eisy || {};
  root.Eisy.types = root.Eisy.types || {};
  root.Eisy.types.loaded = true;
})(typeof globalThis !== 'undefined' ? globalThis : window);
